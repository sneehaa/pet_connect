import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Main UI Colors
  static const Color primaryOrange = Color(
    0xFFFF987C,
  ); // Used for the Login/Signup button
  static const Color backgroundGrey = Color(
    0xFFE9E9E9,
  ); // Used for TextField fill
  static const Color textLightGrey = Color(
    0xFF777777,
  ); // Used for hints and subtitles
  static const Color textDarkGrey = Color(0xFF7B7979); // Used for body text
  static const Color errorRed = Colors.red; // Used for links and error messages
  static const Color primaryWhite = Colors.white;
  static const Color textGrey = Color(0xFF555555);
  static const Color background = Color(0xFFFFF6EC);
}
